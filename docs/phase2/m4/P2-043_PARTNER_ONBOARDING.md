# P2-043 — Partner Auth & Onboarding

Invite token (hashed) → accept on first sign-in. Admins cannot use partner portal; partners cannot escalate to admin. Design + `createPartnerInvite` / `acceptPartnerInvite`.
