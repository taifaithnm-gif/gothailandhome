# P2-022 — Lead Capture Compatibility

Phase 1 forms continue to call `createMarketplaceLead` unchanged.
Additive optional field: `customer_user_id` when signed-in customer submits (flag on).

No breaking field removals. Payload JSON remains open for extras.
