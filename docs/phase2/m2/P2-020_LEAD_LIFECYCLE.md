# P2-020 — Lead Lifecycle Vocabulary

Statuses: see `src/lib/ops/lead-lifecycle.ts` (`LEAD_STATUSES`).
SLA hours: `SLA_HOURS_BY_STATUS`.
Transitions: `canTransitionLeadStatus`.

Outcomes: `LEAD_OUTCOMES`.

**Ops approval:** Frozen for Phase 2A against existing `marketplace_leads` enums.
