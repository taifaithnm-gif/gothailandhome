# P2-033 — Partner Intake UX Upgrade

List-your-property remains Phase 1 form. When `FEATURE_P2_ACQUISITION` is on, submit also creates a tracked `acquisition_cases` row and appends `acquisition=` case id on success URL.

a11y: existing form kit labels retained. i18n: Phase 1 marketplace copy unchanged.
