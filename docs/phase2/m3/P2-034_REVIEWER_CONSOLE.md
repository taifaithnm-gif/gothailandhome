# P2-034 — Reviewer Console Spec

Shell: `/admin/ops/acquisition` (+ detail). Auth: `requireAdmin`. Dual-control: publish bridge requires explicit location_id selection (second control beyond status approve).

Audit: `acquisition_events`.
