# Content Factory — Decision Register

**Companion to:** `CONTENT_FACTORY_ARCHITECTURE_REVIEW.md`
**Date:** 2026-07-17 · **Decisions:** 55 · **P0:** 5 · **P1:** 21 · **P2:** 20 · **P3:** 9

**Revision 2 (2026-07-17):** second independent review pass added CFD-35 … CFD-55.
The pass reached the same verdict and the same five P0s by an independent route,
which raises confidence in the P0 set. Existing IDs CFD-01 … CFD-34 are unchanged.
New findings are schema-integrity and runtime-specific gaps that the first pass did
not reach; they do not overturn any earlier decision, though CFD-35 sharpens
CFD-25/26 and CFD-38 sharpens CFD-04.

**Classifications:** ACCEPT · REVISE BEFORE IMPLEMENTATION · DEFER TO LATER PHASE ·
REMOVE AS OVERENGINEERING · REQUIRES WINDOWS 01 AUDIT DATA

**Severity:** P0 implementation must not start · P1 must be corrected before the
affected phase · P2 important improvement · P3 optional refinement

**Executors:** Architect (design authority) · Data Architect · Platform/Infra ·
Content Ops Lead · Compliance/Legal · CEO (scope authority)

---

## P0 — Implementation must not start

| ID | Document | Section | Finding | Classification | Sev | Required change | Executor | Phase |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **CFD-01** | All six | — | **No runtime architecture exists.** Windows, Docker, WSL, Redis, MinIO, Tailscale, pgvector, Postgres, worker, cron, restart, restore = **0 occurrences in 4,110 lines**. `IMPLEMENTATION_PLAN §23` defers queue, object storage, vector index, and observability to undecided points. The Windows 01 validation this review is meant to unlock **cannot be performed** — there is nothing to validate against. | REVISE BEFORE IMPLEMENTATION | **P0** | Add a runtime/deployment document naming every service, its host, its Windows execution mode (Docker Desktop vs WSL2 vs native), memory ceilings, restart behaviour, and network access. **Specify first, then audit.** | Architect + Platform/Infra | Step 0 |
| **CFD-02** | IMPLEMENTATION_PLAN | §18, §3 | **Pilot is Phase 14 of 15.** Thirteen platform phases — knowledge graph, translation memory, media pipeline, confidence service — precede any contact with a real website. Directly contradicts the brief ("first implementation must remain a small pilot"). Every assumption stays unvalidated for the entire build. | REVISE BEFORE IMPLEMENTATION | **P0** | Invert the plan. Pilot moves to Step 2–4 (see review §9). One article end-to-end before any platform generalisation. | Architect + CEO | Step 0 |
| **CFD-03** | ARCHITECTURE / DATA_MODEL / REVIEW_WORKFLOW | §5.16 / §3 / §3 | **Three incompatible lifecycle vocabularies.** 13 states vs 14 vs 18+6. `localized`/`refreshed` exist only in ARCHITECTURE; `intake_review`/`localization_ready`/`publish_review`/`refresh_required` only in REVIEW_WORKFLOW. `cf_facts.lifecycle_state` and `cf_lifecycle_events` are typed against a vocabulary **no document contains**. No transition table, no guards, no terminal states. | REVISE BEFORE IMPLEMENTATION | **P0** | Define **one** canonical state machine with an explicit legal-transition matrix and guards. All six documents reference it. Separate *lifecycle* state from *review* state if both are needed. | Data Architect + Architect | Step 0 |
| **CFD-04** | PIPELINE | §26, §25 | **Reliability engineering absent.** Total coverage: one sentence ("Retries should be idempotent") + `Retry count` as an observability bullet. **No idempotency key, no job locking/lease, no dead-letter queue, no concurrency control, no restart recovery, no poison-document handling, no file-size ceiling, no rollback procedure.** For a 24×7 single-box runtime these are daily events, not edge cases. | REVISE BEFORE IMPLEMENTATION | **P0** | Add a reliability section: idempotency keys per stage, job lease/lock, DLQ + replay, per-worker concurrency and memory ceilings, restart semantics, poison quarantine, max payload size. | Architect + Platform/Infra | Step 0 (design) · Step 5 (harden) |
| **CFD-05** | DATABASE | §8–§14 | **Multi-tenant isolation undefined.** `cf_sources`/`cf_content_items` carry `tenant_id`; **`cf_entities`, `cf_facts`, `cf_relationships`, `cf_media_assets`, `cf_raw_items` do not.** No RLS, no per-tenant key, no query-scoping rule, no statement of what is shared vs isolated. A GoThailandHome fact is visible to TAI FAITH by default. **Nothing contains a failure to one project** — a bad entity merge is global. | REVISE BEFORE IMPLEMENTATION | **P0** | State explicitly, per table, what is tenant-scoped vs globally shared. Define the isolation mechanism (RLS or enforced scoping) and a failure-containment boundary. | Data Architect | Step 0 |

---

## P1 — Must be corrected before the affected phase

| ID | Document | Section | Finding | Classification | Sev | Required change | Executor | Phase |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **CFD-06** | DATABASE | §9 | **`Claim` has no table.** DATA_MODEL §14 defines it as first-class (`claim_text`, `claim_type`, `support_level`); PIPELINE §9 extracts claims; IMPL §9 lists claim extraction. `cf_claim_conflicts` resolves only `candidate_fact_ids` — it cannot hold a claim conflict. One of the design's best ideas is unstorable. | REVISE BEFORE IMPLEMENTATION | P1 | Either add `cf_claims` + `cf_claim_evidence`, or collapse Claim into `cf_facts` with a `support_level` column. **V1 recommendation: collapse.** All three documents must agree. | Data Architect | Step 0 |
| **CFD-07** | DATABASE | §15 | **Review decisions cannot store what governance requires.** REVIEW_WORKFLOW §2.4 requires `target_version`; §20 additionally requires role, before/after state, and evidence viewed. `cf_review_decisions` has **none of the four**. DATA_MODEL §22 has `target_version` — schema disagrees with both other docs. The `no_silent_overwrite` principle is unenforceable. | REVISE BEFORE IMPLEMENTATION | P1 | Add `target_version`, `reviewer_role`, `state_before`, `state_after`, `evidence_viewed_ids` to `cf_review_decisions`. | Data Architect | Step 0 |
| **CFD-08** | ARCHITECTURE / PIPELINE / DATABASE | §5.15 / §11, §15 / — | **Embedding similarity has no storage and no engine.** Specified as a method in 3 places; **no embeddings table, no vector column, `chunk` = 0 occurrences**; IMPL §23 defers the vector index as undecided. The review brief expects pgvector on Windows 01. | REVISE BEFORE IMPLEMENTATION | P1 | **V1: delete embedding similarity from all method lists.** If retained later: model chunk + embedding objects, choose the engine, and confirm pgvector on the Windows 01 Postgres. | Architect + Data Architect | Step 0 (delete) · later (re-add) |
| **CFD-09** | DATA_MODEL | §29 | **Cross-site entity reuse has no storage.** The showcase pattern (`usable_by_sites`, `site_overlays` with per-site `required_locales`/`review_level`) has **no table**. `cf_source_site_permissions` scopes sources; nothing scopes entities. The single mechanism the "100+ sites" claim rests on exists only as illustrative JSON. | REVISE BEFORE IMPLEMENTATION | P1 | Add `cf_entity_site_overlays` (or equivalent), or delete §29 and stop claiming the capability. | Data Architect | Step 0 |
| **CFD-10** | DATABASE | §4 | **Two naming conventions.** `content_factory_tenants/sites/site_policies` vs `cf_*` for all other tables. Symptom of assembly rather than design; will produce two migrations. | REVISE BEFORE IMPLEMENTATION | P1 | Standardise on `cf_`. | Data Architect | Step 0 |
| **CFD-11** | ARCHITECTURE | §7 | **Robots/ToS enforcement is named, not built.** "Robots and terms review fields" promised; **no table has a robots field.** No `robots_checked_at`, no `robots_allows`, no ToS review record, no crawl-delay, no pipeline enforcement point. The system's legitimacy rests on this control. | REVISE BEFORE IMPLEMENTATION | P1 | Add explicit robots/ToS fields to `cf_sources` + a hard gate in collection planning. **Or** remove the claim while V1 is manual-upload-only, and re-add before any fetch collector. | Compliance/Legal + Data Architect | Step 0 (decide) · before any fetch collector |
| **CFD-12** | ARCHITECTURE / IMPL | §7 / §22 | **Personal data governance is two bullets.** No PII field, no data-subject-rights flow, no erasure mechanism, no PII retention, no lawful basis. **PDPA and GDPR appear nowhere** despite Thai/Malaysian operations and a `jurisdiction` field. Same organisation is already recording `consent_at` against a non-existent privacy policy (CRIT-02) — a demonstrated blind spot. | REVISE BEFORE IMPLEMENTATION | P1 | Add a personal-data section: what PII may enter, detection point, lawful basis, subject rights, erasure cascade, retention. Name PDPA/GDPR explicitly. | Compliance/Legal | Step 0 |
| **CFD-13** | DATABASE | §19 | **Retention policy has no executor.** `archive_after_days`/`delete_after_days`/`legal_hold_allowed` defined; **nothing enforces them.** No job, no schedule, no cascade rules (delete a raw item → what happens to facts citing it, packages quoting it?), no legal-hold override, no verification. A documented-but-unenforced policy is a liability, not a control. | REVISE BEFORE IMPLEMENTATION | P1 | Specify the retention executor, cascade rules across evidence→fact→package, legal-hold override, and verification. | Data Architect + Compliance/Legal | Step 0 (design) · Step 5 |
| **CFD-14** | IMPLEMENTATION_PLAN | §22 | **Backup is one bullet; restore is absent.** No RPO, no RTO, no target, no restore test, no recoverability statement. On a single-box 24×7 runtime this is the difference between an incident and terminal data loss. **Backup without a tested restore is not a backup.** | REVISE BEFORE IMPLEMENTATION / REQUIRES WINDOWS 01 AUDIT DATA | P1 | Define RPO/RTO, backup target (off Windows 01), and a **verified restore** as a Step 3 exit gate. | Platform/Infra | Step 1 + Step 3 |
| **CFD-15** | REVIEW_WORKFLOW | §19, §4 | **Reviewer capacity is unmodelled.** SLAs (1/2/5 business days) and 10 roles are specified with **no estimate of reviewer headcount, items/day capacity, or pipeline output rate**. IMPL §25 names "review backlog at scale" as a risk and never sizes it. Realistic V1 reviewer population: **1–3 people.** Human review is the system's binding constraint — not compute. | REVISE BEFORE IMPLEMENTATION | P1 | Add a throughput model: candidates/day produced vs items/day reviewable. Size V1 to actual headcount. Delete SLAs until staffed (CFD-27). | Content Ops Lead + CEO | Step 0 |
| **CFD-16** | ARCHITECTURE / all | §6, §9 | **"100+ websites" is unsubstantiated** and appears in 5 of 6 documents. No volume, throughput, reviewer-capacity, or cost model (`throughput`/`volume`/`cost` = 0 occurrences). It is the root justification for the 15 entity types, 10 roles, 6 levels, and 22 stages that make V1 undeliverable. Real portfolio: 5 sites, 2 hypothetical, 1 mid-Alpha with an open P0. | REMOVE AS OVERENGINEERING | P1 | Replace with: *"a second site must not require rewriting the pipeline."* **Design for 2, not 100.** Two sites prove reuse; 100 is a marketing number. | CEO + Architect | Step 0 |
| **CFD-17** | IMPLEMENTATION_PLAN | §5, §6 | **Phase ordering contradiction.** Phase 1 exit requires "sample fixtures can move from **source registration** to parsed document" — but source registry is **Phase 2**. Phase 1 depends on a phase that follows it. | REVISE BEFORE IMPLEMENTATION | P1 | Merge Phases 1–2, or move the minimal source registry into Phase 1. | Architect | Step 0 |
| **CFD-18** | IMPLEMENTATION_PLAN | §24 vs §9–§14 | **MVP scope contradicts phase order.** §24's MVP includes entity/fact candidates, review tasks, confidence scoring v1, duplicate detection v1, glossary v1 — compressing Phases 5, 6, 9, 10, 11 into "MVP" while those phases are separately sequenced. Two incompatible plans in one document. | REVISE BEFORE IMPLEMENTATION | P1 | Make §24 the authoritative V1 boundary (aligned to review §8) and re-sequence the phases beneath it. Delete the duplicate plan. | Architect | Step 0 |
| **CFD-19** | DATABASE | §2.4 vs §8, §9 | **"Append-only history" contradicts mutable rows.** Design rule 4 states material revisions create versions rather than overwriting; `cf_entities`, `cf_facts`, and `cf_content_items` all carry `updated_at`, implying in-place mutation. Unclear which fields are immutable and what triggers a version. | REVISE BEFORE IMPLEMENTATION | P1 | State per table: immutable, versioned, or mutable — and what constitutes a "material" change that forces a version. | Data Architect | Step 0 |
| **CFD-35** | PIPELINE / DATA_MODEL / DATABASE | §14 / §13, §24 / §18 | **Confidence is two incompatible scales.** Floats in [0,1]: `parse_confidence` 0.92, alias 0.88, fact 0.95, relationship 0.94, classification 0.98, content item 0.84. Integers in [0,100]: `cf_confidence_scores.score` 92, `quality_score` 88, and **every band in PIPELINE §14** (`90-100`/`75-89`/`60-74`/`40-59`/`0-39`). `cf_content_items` carries **both on one row** (`confidence_score` 0.84, `quality_score` 88). Every threshold, band, and gate rule in the design is therefore ambiguous. Sharpens CFD-25/26 — those defer or collapse scoring but do not catch the type conflict, which survives any simplification. | REVISE BEFORE IMPLEMENTATION | P1 | Pick one scale and one type. **Recommendation: integer 0–100 everywhere**, since the bands are already expressed that way. State it once as a field convention. | Data Architect | Step 0 |
| **CFD-36** | DATABASE / ARCHITECTURE / PIPELINE | §16 / §5.10 / §20 | **`cf_publish_packages` cannot express its own package types.** The table is keyed on a required `content_item_id`, but the declared package types include **Taxonomy package, Site feed package, Media package, and Entity profile package** — none of which are content items. Roughly half the publishing surface named in three documents has no storage path. | REVISE BEFORE IMPLEMENTATION | P1 | Generalise to `target_type` + `target_id` (matching the polymorphic convention already used by `cf_classifications`, `cf_localizations`, `cf_media_links`), **or** cut V1 package types to `content_item` only and delete the rest from all three documents. **V1 recommendation: cut.** | Data Architect | Step 0 |
| **CFD-37** | DATABASE | §8 (absent) | **No entity merge/redirect table.** Duplicate review can "Confirm duplicate and merge" (`REVIEW_WORKFLOW §12`), PIPELINE §24 lists "Entity merged" as an archive trigger, and REVIEW_WORKFLOW §10 offers "Split incorrect merge". Nothing records surviving ID, merged ID, or redirect. **Published packages and adapters holding a merged entity's ID break silently**, and a split is unrecoverable without merge lineage. Entity merge is the one destructive operation in the design and it is the one with no audit trail. | REVISE BEFORE IMPLEMENTATION | P1 | Add `cf_entity_merges` (`surviving_entity_id`, `merged_entity_id`, `merged_by`, `merged_at`, `reason_code`, `reversible`). Resolve merged IDs through it at package assembly. | Data Architect | Step 0 |
| **CFD-38** | DATABASE / DATA_MODEL / PIPELINE | §6 / §2 / §25 | **No pipeline execution record and no error object.** CFD-04 covers the missing reliability *controls*; this is the missing *storage*. `cf_collection_jobs` models **collection only** — parse, normalize, extract, resolve, classify, score, localize, and publish stages have **no run record**, though PIPELINE §25 requires every stage emit status, timestamps, actor, input/output IDs, error code, and retry count. `DATA_MODEL §2` object families omit both **`chunk`** and **`error`**; the only "task" modelled is a *review* task. The brief's data-model checklist names both `task` and `error` as required objects. | REVISE BEFORE IMPLEMENTATION | P1 | Add `cf_job_runs` (stage-agnostic: `stage`, `target_type`, `target_id`, `attempt`, `lease_expires_at`, `idempotency_key`, `status`) and `cf_errors` (`job_run_id`, `error_code`, `detail`, `is_poison`). These are the tables CFD-04's controls need in order to exist. | Data Architect + Platform/Infra | Step 0 |
| **CFD-39** | ARCHITECTURE / DATABASE | §5.13 / §7 | **Parsed documents are declared versioned; no version table exists.** ARCHITECTURE §5.13 lists "Parsed documents" among versioned objects. `cf_raw_item_versions` exists for the raw layer; the parsed layer has no analogue and no uniqueness rule. Re-parsing a raw item with a new `parser_version` — which every parser upgrade triggers across the whole corpus — either overwrites prior output (breaking `cf_fact_evidence.document_section_id` citations) or silently duplicates. Parser upgrades are routine, so this is a first-year problem, not a later one. | REVISE BEFORE IMPLEMENTATION | P1 | Add `cf_parsed_document_versions` **or** make `(raw_item_id, parser_type, parser_version)` unique and treat each parse as a new immutable row. Define what happens to facts citing superseded sections. | Data Architect | Step 0 |
| **CFD-40** | PIPELINE / REVIEW_WORKFLOW / ARCHITECTURE | §14 / §21, §2.7 / §2.3 | **Reviewer validation is weighted 5 of 100 — below recency at 10.** A human reviewer verifying a fact against a government source contributes half of what the calendar contributes. This contradicts ARCHITECTURE §2.3 and REVIEW_WORKFLOW §2.7 ("AI assists, humans govern"). Worse, it is **actively unsafe in combination with REVIEW_WORKFLOW §21**, which reopens review when "confidence score drops below threshold": recency decay alone can drag a human-approved, evidence-backed government fact below threshold and auto-reopen it, generating review churn that punishes the reviewer for having done the work. | REVISE BEFORE IMPLEMENTATION | P1 | Reviewer approval must **dominate or be a separate axis** from the computed score — a verified fact does not decay because time passed. If scoring is deferred per CFD-25, carry this constraint into whatever replaces it. | Architect + Content Ops Lead | Step 0 |
| **CFD-41** | PIPELINE / DATABASE | §26 / — | **No resource ceilings.** No max payload size, no per-worker memory cap, no stage timeout, no page/sheet count limit, no poison-document quarantine threshold. A 400MB scanned PDF or a 50k-row Excel workbook has undefined behaviour on a single box shared with Postgres. Named in the brief (oversized files, poison documents); absent from the documents. | REVISE BEFORE IMPLEMENTATION | P1 | Specify max payload size at intake (reject before storage, not after), per-worker memory ceiling, per-stage timeout, and an attempt threshold after which an item is quarantined as poison rather than retried forever. | Platform/Infra | Step 0 (design) · Step 5 |

---

## P2 — Important improvements

| ID | Document | Section | Finding | Classification | Sev | Required change | Executor | Phase |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **CFD-20** | ARCHITECTURE vs REVIEW_WORKFLOW | §5.9 / §4 | **Reviewer roles: 7 vs 10.** Workflow adds Entity, Relationship, and Duplicate reviewers absent from the architecture's component list. | REVISE BEFORE IMPLEMENTATION | P2 | Reconcile. **V1: one role** (CFD-24). | Architect | Step 0 |
| **CFD-21** | ARCHITECTURE vs PIPELINE | §5.14 / §14 | **Confidence signals: 10 vs 8.** ARCHITECTURE lists `duplicate_risk` and `media_rights_confidence`; PIPELINE's 8 components already sum to exactly 100, so adding them forces re-weighting. Not a shared model. | REVISE BEFORE IMPLEMENTATION | P2 | Reconcile — or defer scoring entirely (CFD-25). | Architect | Step 0 |
| **CFD-22** | DATA_MODEL vs DATABASE | §11 / §8 | **Entity `review_status` missing from schema.** Model has it; `cf_entities` has only `status`. PIPELINE §11's hard gate ("high-impact entities require review before public use") is unenforceable without it. | REVISE BEFORE IMPLEMENTATION | P2 | Add `review_status` to `cf_entities`. | Data Architect | Step 0 |
| **CFD-23** | DATA_MODEL vs DATABASE | §17 / §12 | **Content-item provenance modelled twice.** `source_ids` array vs the `cf_content_item_sources` join table. Two sources of truth for provenance — the one thing this system exists to get right. | REVISE BEFORE IMPLEMENTATION | P2 | Keep the join table; delete the array. | Data Architect | Step 0 |
| **CFD-24** | REVIEW_WORKFLOW | §4, §5, §6, §16 | **10 roles, 9 task types, 6 levels (L0–L5), 3 risk classes** — a content-operations department's permission matrix aimed at a team of 1–3. Largest single source of unnecessary complexity. | REMOVE AS OVERENGINEERING | P2 | **V1: one reviewer role; approve / reject / needs-work.** Re-introduce roles only when headcount exists. | Content Ops Lead + Architect | Step 0 · revisit Step 6 |
| **CFD-25** | PIPELINE | §14 | **8-component weighted confidence model with 5 bands.** Weights (20/15/15/15/10/10/10/5) are invented and unvalidated; bands imply a 73 and a 76 differ meaningfully with no evidence the model discriminates at all. False precision. | DEFER TO LATER PHASE | P2 | **V1: source authority + has-evidence + reviewer-approved.** Defer scoring until there is data to calibrate against. | Architect | Defer to Step 6+ |
| **CFD-26** | DATABASE | §18 | **Two parallel scoring systems.** `cf_confidence_scores` and `cf_quality_scores`, each with breakdown + version, with no stated interaction and no rule for which gates publication. | REMOVE AS OVERENGINEERING | P2 | Collapse to one score. | Data Architect | Step 0 |
| **CFD-27** | REVIEW_WORKFLOW | §19 | **SLA table with no capacity model** (see CFD-15). Business-day targets with nothing behind them. | REMOVE AS OVERENGINEERING | P2 | Delete until reviewer capacity is known and staffed. | Content Ops Lead | Step 0 |
| **CFD-28** | REVIEW_WORKFLOW | — | **No batch review.** The brief asks for it; the workflow has no bulk operations. With one reviewer, per-item review is the difference between viable and not. | REVISE BEFORE IMPLEMENTATION | P2 | Add batch approve-by-source, batch merge, batch reject to **V1**. | Content Ops Lead + Architect | Step 0 (design) · Step 4 |
| **CFD-29** | DATABASE | §6, §17 | **Dual versioning.** `cf_object_versions` (generic snapshots) and `cf_raw_item_versions` (specific), with no rule for which applies to what. | REMOVE AS OVERENGINEERING | P2 | Keep `cf_object_versions` only. | Data Architect | Step 0 |
| **CFD-30** | DATABASE / DATA_MODEL | §8 / §11 | **15 entity types** including `sport_event` and `trade_lead` for two non-existent websites. Each type is a schema commitment, review path, and test surface. | REMOVE AS OVERENGINEERING | P2 | **V1: 3 types** — `organization`, `project`, `location`. Add types when a site needs one. | Data Architect | Step 0 |
| **CFD-42** | DATABASE | §13 | **Dangling glossary version reference.** `cf_localizations.glossary_version_id` points at a glossary *version* table that does not exist; `cf_glossary_terms` has no version column. | REVISE BEFORE IMPLEMENTATION | P2 | Drop the field with the localization deferral (CFD-34), or add glossary versioning when localization re-enters. | Data Architect | Defer with CFD-34 |
| **CFD-43** | PIPELINE / REVIEW_WORKFLOW / DATABASE | §21 / §16 / §13 | **"Required locales complete" is unverifiable.** Both the publishing-approval check and the publishing review require it. `cf_localizations` is an EAV table (`target_type`, `target_id`, `field_name`, `localized_value`); evaluating completeness requires counting rows against a **per-site, per-content-type required-field manifest that does not exist**. The gate cannot be computed. | REVISE BEFORE IMPLEMENTATION | P2 | Add a required-field manifest per site × content type × locale, or express it in `cf_site_policies` under `locale_requirement`. Deferred at V1 (single locale), but the gate must not be claimed until it is computable. | Data Architect | Defer with localization |
| **CFD-44** | REVIEW_WORKFLOW | §21 | **"Source changes materially" has no definition.** It is the primary reopen trigger for published content. The only hook is `cf_raw_item_versions.diff_summary`, whose semantics are undefined. A whitespace change and a changed legal threshold are indistinguishable — so the trigger either fires on everything (review churn) or is quietly disabled. | REVISE BEFORE IMPLEMENTATION | P2 | Define materiality concretely: section-hash change on a section cited by an approved fact ⇒ material. Uncited-section changes ⇒ log only. This makes `cf_document_sections.section_hash` load-bearing, which is the right use of it. | Architect + Content Ops Lead | Step 0 |
| **CFD-45** | DATABASE / IMPLEMENTATION_PLAN | §16 / §17 | **No publish rollback path.** Phase 13 names a "Rollback or supersession path" as core work; `cf_publish_events.event_type` is unenumerated, and no mechanism lets an adapter revert to `package_version` N−1. The brief names rollback explicitly. Supersession (forward) is designed; rollback (backward) is not. | REVISE BEFORE IMPLEMENTATION | P2 | Enumerate `event_type` (`published`, `failed`, `rolled_back`, `unpublished`, `superseded`) and define adapter-side revert to the prior package version. | Architect | Step 0 (design) · Step 4 |
| **CFD-46** | DATABASE | §16 | **`cf_publication_channels` is redundant** with `cf_sites.publishing_adapter` + `cf_publish_packages.site_id`. A channel abstraction layered on an adapter abstraction, for one pilot site with one adapter. | REMOVE AS OVERENGINEERING | P2 | Remove from V1. Re-add if a site ever publishes to two destinations. | Data Architect | Step 0 |
| **CFD-47** | DATABASE | §4 | **`content_factory_tenants` is management overhead for a single operator** — status workflow, default locale, lifecycle, for one tenant. Note this is **narrower than it looks**: CFD-05 requires `tenant_id` **columns** on the core tables, and that requirement stands. What is deferred is tenant *management*, not tenant *scoping*. | DEFER TO LATER PHASE | P2 | Keep `tenant_id` columns everywhere per CFD-05; hardcode one tenant row; defer tenant management, workflow, and UI. | Data Architect | Step 0 |
| **CFD-48** | DATABASE | §11 | **Three-table taxonomy** (`cf_taxonomies` + `cf_taxonomy_terms` + `cf_classifications`) with hierarchy, localized labels, scopes, and per-assignment confidence and review — before one article is published. | DEFER TO LATER PHASE | P2 | **V1: a flat tag list.** Re-introduce the taxonomy engine when a second site's topic model actually diverges. | Data Architect | Defer to Step 6+ |
| **CFD-49** | DATABASE | §15 | **`cf_review_checklists` with `checklist_json`** — configurable per-site, per-content-type, per-review-type checklist definitions, for a team of one to three. The checklists in `REVIEW_WORKFLOW §8–§16` are already written as prose and are perfectly usable as prose. | DEFER TO LATER PHASE | P2 | Keep the checklists in Markdown. Add the table when reviewers exist to configure it for. | Content Ops Lead | Defer to Step 6+ |
| **CFD-50** | ARCHITECTURE / PIPELINE | §5.2 / §5 | **Email/mailbox collector** is listed as a collector family for approved inbound documents. There is no mailbox, no inbound document flow, and no source registered against one. A source class with no source. | REMOVE AS OVERENGINEERING | P2 | Remove from the collector families in both documents. | Architect | Step 0 |

---

## P3 — Optional refinements

| ID | Document | Section | Finding | Classification | Sev | Required change | Executor | Phase |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **CFD-31** | DATA_MODEL vs DATABASE | §20 / §14 | **Media alt text: value vs status.** Model stores localized `alt_text` on the asset; schema stores only `alt_text_status`, implying text lives in `cf_localizations`. An implementer will build both. | REVISE BEFORE IMPLEMENTATION | P3 | Pick one — `cf_localizations` is the consistent choice. (Media is deferred at V1 anyway.) | Data Architect | Defer to media phase |
| **CFD-32** | ARCHITECTURE / PIPELINE | §5.15 / §15 | **9 duplicate detection methods** — including perceptual hashing and embedding similarity — before exact-hash dedup has run once. | DEFER TO LATER PHASE | P3 | **V1: content hash + canonical URL.** Add methods when duplicates are actually observed. | Architect | Defer to Step 5+ |
| **CFD-33** | DATABASE | §5 | **`cf_source_credentials`** stores secret references for API collectors that IMPL §24 explicitly defers. Attack surface for an unbuilt capability. | REMOVE AS OVERENGINEERING | P3 | Remove from V1; re-add with the API collector. | Data Architect | Defer to API collector phase |
| **CFD-34** | DATABASE / PIPELINE | §13 / §18 | **Translation memory + glossary + TM match IDs** — a localization industry stack before one article has been published. | DEFER TO LATER PHASE | P3 | Defer entirely. Localization re-enters when a second locale is actually required for published content. | Architect | Defer to Step 6+ |
| **CFD-51** | DATABASE / DATA_MODEL | §14 / §20 | **`approved_with_attribution` has nowhere to store the attribution.** It is a valid `rights_status`, but `cf_media_assets` has no `attribution_text` field and `cf_publish_packages.package_payload.citations` carries source citations only — not media credits. The rights status can be set and the legal obligation it encodes cannot be discharged. | REVISE BEFORE IMPLEMENTATION | P3 | Add `attribution_text` to `cf_media_assets` and carry it into package payloads. (Media deferred at V1.) | Data Architect | Defer to media phase |
| **CFD-52** | DATABASE | §8 | **Circular FK.** `cf_entities.created_from_evidence_id` → `cf_entity_evidence.id` → `cf_entity_evidence.entity_id` → `cf_entities.id`. Neither row can be inserted first without a nullable column or a deferred constraint. | REVISE BEFORE IMPLEMENTATION | P3 | Make it nullable and populate after insert, or drop it — the link is already reachable via `cf_entity_evidence`. | Data Architect | Step 0 |
| **CFD-53** | DATABASE | §20 | **Index list omits the indexes the design's own methods require.** No vector index (embedding similarity), no trigram/GIN index (normalized title and text similarity) — both named as required methods in `ARCHITECTURE §5.15` and `PIPELINE §15`. Consistent with CFD-08: the similarity capability is specified in prose and absent from the schema in every respect. | REVISE BEFORE IMPLEMENTATION | P3 | Resolves with CFD-08. If similarity is deleted at V1, the index list is already correct. | Data Architect | Step 0 (with CFD-08) |
| **CFD-54** | PIPELINE | §15 | **"False positive: reviewer dismissal saved as training signal."** No training loop, model, retraining cadence, or consumer of that signal exists anywhere in six documents. It reads as capability that does not exist. | REMOVE AS OVERENGINEERING | P3 | Delete the phrase, or restate honestly as "recorded for later analysis". | Architect | Step 0 |
| **CFD-55** | ARCHITECTURE | §5.11 | **"The factory must not assume only EN, ZH, and TH."** Every site in the portfolio — actual and hypothetical — is EN/ZH/TH. Generality bought against no requirement. Harmless as a design note; harmful if it justifies locale machinery (CFD-34). | REMOVE AS OVERENGINEERING | P3 | Keep locale as a configurable field (it already is); drop the injunction and the machinery it motivates. | Architect | Step 0 |

---

## Summary

| Severity | Count | Meaning |
| --- | --- | --- |
| **P0** | 5 | Implementation must not start (CFD-01 … CFD-05) |
| **P1** | 21 | Must be corrected before the affected phase |
| **P2** | 20 | Important improvement |
| **P3** | 9 | Optional refinement |
| **Total** | **55** | |

| Classification | Count |
| --- | --- |
| REVISE BEFORE IMPLEMENTATION | 38 |
| REMOVE AS OVERENGINEERING | 11 |
| DEFER TO LATER PHASE | 6 |
| REQUIRES WINDOWS 01 AUDIT DATA | 1 (CFD-14; CFD-01 must be resolved before any audit is meaningful) |
| ACCEPT | 0 — no finding was accepted as-is; the design's strengths are recorded in review §2 rather than as decisions |

Classification counts sum to 56 across 55 decisions because CFD-14 carries two
(REVISE + REQUIRES WINDOWS 01 AUDIT DATA). *Revision 2 note: the revision-1 summary
under-counted REVISE as 20 against 24 actual rows; corrected here.*

**Critical path:** CFD-01 (name the runtime) and CFD-16 (delete the "100+" premise)
unblock most of the rest. Both are documentation decisions costing no engineering
time — and GoThailandHome's open P0 (`PRODUCTION_FIX_PLAN.md` CRIT-01) should hold
engineering capacity until it is closed.

**Sequence dependency:** CFD-01 must be resolved **before** the Windows 01 audit.
You cannot audit a machine for services no document has named.

**On the revision-2 additions.** The 21 new decisions are almost entirely
**schema-integrity** findings — a table that cannot store its own declared outputs
(CFD-36), a destructive operation with no audit trail (CFD-37), a scale that is two
types at once (CFD-35), a versioning claim with no version table (CFD-39), missing
execution and error tables (CFD-38). They cluster in `DATABASE.md`, and the pattern
is consistent with CFD-10's diagnosis: **that document was assembled from the others
rather than derived from them.** It is the weakest of the six and should be rewritten
against the reconciled state machine (CFD-03) rather than patched finding by finding.

Note the net effect on scope is close to neutral. Revision 2 adds five required
tables (`cf_job_runs`, `cf_errors`, `cf_entity_merges`, `cf_parsed_document_versions`,
and chunks/embeddings if CFD-08 is retained) while removing or deferring seven
(`cf_publication_channels`, `cf_review_checklists`, the three taxonomy tables, and
the glossary/TM pair). **The V1 table count does not grow — it redistributes from
conceptual surface to operational floor**, which is precisely the trade the P0 set
calls for.
